# Dependências completas do benchmark (backend Azure + embeddings locais opcionais).
# Para rodar só com o gateway Azure e sem GPU, use requirements-azure.txt.

numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
networkx>=3.0
pandas>=2.0

openai>=1.40           # cliente Azure OpenAI / OpenAI-compatível
httpx>=0.27            # necessário para o CA bundle corporativo
tiktoken>=0.7.0        # contagem de tokens; sem ele a contagem vira estimativa
tqdm>=4.66

rank-bm25>=0.2.2       # baseline BM25
PuLP>=2.7,<4              # ILP da seleção de memória (traz o CBC)
python-igraph>=0.11    # Leiden para as comunidades do GraphRAG

# Opcional: embeddings locais. Sem estes, o código usa o deployment Azure de
# embedding ou cai para TF-IDF.
# sentence-transformers>=3.0
# torch>=2.1
