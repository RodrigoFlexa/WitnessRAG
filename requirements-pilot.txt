# Ambiente Linux do benchmark (pode ser separado do ambiente vLLM).
-r requirements.txt
sentence-transformers>=3.0
matplotlib>=3.7
# Instale vllm num venv separado compatível com CUDA/driver e use --vllm-python.
