# Mínimo para rodar tudo usando SÓ o gateway Azure (chat + embeddings).
# Sem torch, sem transformers, sem sentence-transformers.
#
#   python -m venv venv && source venv/bin/activate
#   pip install -r requirements-azure.txt

numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
networkx>=3.0

openai>=1.40
httpx>=0.27
tiktoken>=0.7.0
tqdm>=4.66

rank-bm25>=0.2.2
PuLP>=2.7,<4
python-igraph>=0.11
